"use client";

// Signal / Shelf direction: bridge the existing product-first landing page into
// the Next.js App Router without changing its editorial sections or interactions.
import Home from "../client/src/pages/Home";

export default function Page() {
  return <Home />;
}
